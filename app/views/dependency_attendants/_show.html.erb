<legend>
    Informacion de la dependencia
</legend>

<dl class="dl-horizontal">
    <% if !current_dependency_attendant.nil? %>
</dl>
<%= button_to "Editar informacion de la dependencia", edit_dependency_attendant_path(current_dependency_attendant), method: :get, class:"btn btn-primary" %>
<% else %>
<small class="muted">
    La informacion de la dependencia no ha sido actualizada aun.
</small>
<br/>
<%= button_to "Agregar informacion de la dependencia", new_dependency_attendant_path,
    method: :get, class:"btn btn-primary" %>
<% end %>
