// Place all the styles related to the Prueba controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/

ul.horizontal-slide {
    margin: 0;
    padding: 0;
    width: 100%;
    height: 100px;
    white-space: nowrap;
    overflow-x: auto;
    list-style-type: none; 
    background: #eaeaea;
    border:2px solid gray;
    border-radius:5px;
}

ul.horizontal-slide li[class*="span"] {
    border:2px solid #ef6e3e;
    border-radius:5px;
    display: inline-block;
    font-size: 12px;
    height:100px;
    float: none;
    background: #ffffff;
    color: #000000;
    &:hover {
      background: #e0e0ff;
  }
}

ul.horizontal-slide li[class*="span"]:first-child {
    margin-left: 0;
}
td{
  font-size: 12px;
}

